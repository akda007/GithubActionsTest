{
  "name": "cpftest",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "jest --testEnvironment=node --watchAll --verbose --coverage",
    "start": "node index.js",
    "dev": "nodemon index.js",
    "deploy": "jest --testEnvironment=node --verbose --detectOpenHandles",
    "build": "set CI=false && npm i"
  },
  "author": "",
  "license": "ISC",
  "dependencies": {
    "babel": "^6.23.0",
    "jest": "^29.7.0"
  },
  "babel": {
    "presets": [
      "@babel/preset-env"
    ]
  },
  "devDependencies": {
    "@babel/core": "^7.24.4",
    "@babel/preset-env": "^7.24.4"
  }
}
